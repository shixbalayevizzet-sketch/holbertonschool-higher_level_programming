// Select the first <header> element on the page
const headerElement = document.querySelector('header');

// If the element exists, change its text color to red (#FF0000)
if (headerElement) {
    headerElement.style.color = '#FF0000';
}
